server:
  api_port: 8080

receiver:
  grpc_port: 4317
  http_port: 4318

processor:
  batch_size: 1000
  flush_interval: "2s"
  queue_size: 10000
  drop_on_full: true

storage:
  path: "./data/apm.db"
  retention_days: 7
